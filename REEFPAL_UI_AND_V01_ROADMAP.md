# ReefPal UI 统一性检查与 V0.1 三阶段路线

> 更新日期：2026-08-20  
> 当前项目快照：FastAPI 0.5.0、SQLite 备份结构 v5、145 项自动化测试、纯 HTML/CSS/JavaScript 单页前端。  
> 本文目的：确定 ReefPal 的视觉方向，并把“一次性补充闭环 → 手机实测验收 → PWA 与数据可靠性”三阶段拆成可以逐项执行的框架。

---

## 实施状态（随代码同步更新）

| 编号 | 工作项 | 状态 | 当前说明 |
|---|---|---|---|
| UI-01 | 阳光潮池语义色与页面背景骨架 | 已完成 | 已建立语义 Token，覆盖页面背景、主表面、品牌区和焦点卡 |
| UI-02 | 压缩品牌区与鱼缸上下文 | 已完成 | 帮助与备份并入应用栏；手机鱼缸上下文压到约 44px |
| UI-03 | 今日海况跨模块压缩 | 已完成 | 水质页保留完整版，换水/补充/滴定使用约 55px 礁况摘要条 |
| UI-04 | 统一主导航 SVG 图标 | 已完成 | 四个主入口已换成本地内联线性 SVG，不再依赖系统 Emoji |
| UI-05 | 午夜礁盘深色 Token | 未开始 | 在亮色组件收口后实施 |
| UI-06 | 桌面双栏工作区 | 未开始 | 等手机首屏结构稳定后实施 |
| UI-07 | 配盐默认值与目标比重细调 | 已完成 | 通用参考 35g/L、实际水量 20%、按需展开 1.022–1.028 细调轮盘 |
| UI-08 | 入门指南结构与文案重做 | 已完成 | 三步起步路径、四入口速查、能力边界与直接跳转均已落地 |
| FLOW-01 | 一次性补充闭环 | 未开始 | UI 首批收口后进入阶段一 |
| MOBILE-01 | Android / 鸿蒙 / iOS 真实设备验收 | 未开始 | 模拟视口通过后再进入真实设备 |
| PWA-01 | Service Worker 与只读离线壳 | 未开始 | 阶段三 |

### 本轮实施记录

- 2026-08-19：开始 UI 第一批改造，范围为阳光潮池基础、紧凑应用栏、鱼缸上下文压缩、礁况摘要条和主导航 SVG 化。
- 2026-08-19：UI-01 至 UI-04 完成。390 × 844 模拟视口中，三个次级模块礁况条约 55px，模块内容进入首屏；页面宽度未超过视口。桌面 951px 视口无页面级横向溢出。
- 2026-08-19：新增 UI 结构回归测试，完整测试结果为 143 passed；仅保留现有 TestClient 依赖弃用警告。
- 2026-08-20：开始 UI-07，降低配盐计算的品牌选择负担，并增加不常驻页面的目标比重微调。
- 2026-08-20：UI-07 完成。46 L 实际水量默认带入 9.2 L；不选品牌按 35 克/升得到 322 克，目标比重调至 1.026 后联动为 36.4 克/升、335 克。390 × 844 模拟视口完成展开/收起与布局检查，完整回归 144 passed。
- 2026-08-20：开始 UI-08，重做入门指南的信息顺序与文案，不再按功能说明书的方式介绍产品。
- 2026-08-20：UI-08 完成。指南改为“三步开始 + 四入口速查”，补充今日海况的数据来源边界；四个入口可直接跳转。390 × 844 模拟视口一屏完整展示，无内部滚动和底栏遮挡；完整回归 145 passed。

---

## 一、先说结论

ReefPal 现在不是“难看”，而是已经有一套整洁的海水蓝界面，却还没有形成足够鲜明的产品性格。当前最大问题并非颜色不够丰富，而是：

1. 大部分内容都使用白色圆角卡片、浅阴影和蓝色按钮，模块职责不同，视觉待遇却很接近。
2. 波浪、水滴和 Emoji 能表达“和水有关”，但比较通用，还没有成为 ReefPal 独有的识别符号。
3. 手机端虽然有底部导航，但顶部品牌区、鱼缸铭牌和完整“今日海况”在四个模块重复出现，真正要操作的内容经常被推到首屏以下。
4. 当前前端约 4184 行，HTML、CSS、JavaScript 集中在一个文件中；颜色变量已经有雏形，但仍存在大量硬编码白色、背景色和局部样式，直接加深色模式会很容易漏色。
5. 当前使用系统默认字体，标题、数据、说明之间主要靠粗细区分。数字很多的产品需要更明确的“标题字、正文字、数据字”节奏。

建议不做两套互相独立的界面，而是先建立一套共用的语义化设计系统，再提供：

- 默认亮色：“阳光潮池 · Reef Field Notes”
- 可选深色：“午夜礁盘 · Night Reef Console”

两版共用布局、图标、间距和组件，只切换颜色、表面材质、图表配色与少量背景效果。亮色适合白天和新手，深色适合夜间维护、老玩家和 OLED 屏幕。主题默认跟随系统，同时保留手动选择；用户手动选择后，以用户选择为准。

ReefPal 最值得建立的记忆点，不是再加一张鱼或珊瑚插图，而是把现有的“今日潮位线”升级为产品签名组件：**礁况线 Reef Pulse**。它同时表达当前状态、风险程度、复测节点和维护节奏，并在首页、趋势图、补充复测卡片中保持同一种视觉语言。

---

## 二、当前 UI 实机检查

### 2.1 检查方式

本次检查了当前本地页面：

- 桌面视口：约 917 × 1272
- 手机视口：390 × 844
- 水质、元素补充、滴定三个主要页面
- 顶部品牌区、鱼缸铭牌、今日海况、底部导航、表单、状态卡、元素卡和模块说明

手机端没有发现页面级横向滚动；图表 SVG 有少量超出视口的内部绘制区域，但未造成 `body` 横向溢出。响应式基础是合格的。

### 2.2 已经做得不错的地方

| 项目 | 当前表现 | 应当保留 |
|---|---|---|
| 功能结构 | 水质、换水、补充、滴定四个核心入口明确 | 保留四入口，不在主导航继续加栏目 |
| 手机导航 | 底部导航已接近原生 App 使用习惯 | 保留位置，统一图标与选中态 |
| 鱼缸上下文 | 鱼缸类型、阶段、水量集中在一张铭牌 | 保留，但进一步压缩高度 |
| 今日状态 | 结论在前，依据和维护节奏可以展开 | 保留逻辑，重做跨页面展示方式 |
| 专业/简洁模式 | 新手和老手能选择不同信息密度 | 保留，并明确两种模式各自职责 |
| 状态颜色 | 正常、留意、风险已有颜色区分 | 保留语义，统一色值和对比度 |
| 表单保护 | 输入框、焦点态、手机键盘类型已有考虑 | 继续按组件标准收口 |
| 文案气质 | 已从“系统命令”调整为自然、严谨的玩家语言 | 作为全产品文案基准 |

### 2.3 影响“独特感”的问题

#### A. 视觉层级过于平均

鱼缸铭牌、今日海况、录入表单、趋势卡、补充卡和滴定卡大量使用相近的白底、20px 左右圆角和阴影。用户能看懂每一块，但第一眼不容易判断“今天最重要的是什么”。

改进方向：

- 只给“今日结论”和“当前主要动作”使用强表面。
- 普通信息区改为无阴影分组或仅使用细边框。
- 历史记录、方法说明、证据来源采用折叠层，不与操作卡抢视觉。
- 同一屏最多出现一个大阴影焦点卡，其余卡片降低层级。

#### B. 手机首屏被公共区占满

在 390 × 844 视口中，切到“补充”或“滴定”后，当前模块的内容顶部大约从页面 648px 处才开始。用户明明已经切换模块，却仍要先越过完整“今日海况”。

改进方向：

- “水质/首页”保留完整今日海况。
- “换水、补充、滴定”只显示一条 48–56px 的礁况摘要条，例如“PO₄ 待复测 · 今天 1 项”。
- 点击摘要条再展开完整海况，不让公共信息长期占据其他任务的首屏。
- 切换底部导航后将当前模块标题和首要操作置于首屏，而不是让用户再次滚动。

#### C. 品牌装饰比较通用

顶部波浪、漂浮水滴、蓝色渐变都能说明“海水”，但类似元素也常见于天气、饮水、泳池和环保产品。

改进方向：

- 删除持续漂浮的大水滴，减少“网页模板装饰感”。
- 背景改用更克制的水下焦散、盐度刻度、珊瑚骨骼网格等细节。
- 把“礁况线 Reef Pulse”作为唯一大胆识别点，其余装饰退后。
- Logo 后续可围绕“水面线 + 珊瑚枝 + 测试曲线”重新绘制，而不是只依赖波浪图形。

#### D. Emoji 体系不稳定

当前标题和导航中存在多种 Emoji。它们亲切，但在 Android、鸿蒙、iOS 和 Windows 上形态不同，容易让正式产品显得拼贴。

改进方向：

- 主导航、模块标题、状态和按钮统一使用一套 SVG 线性图标。
- Emoji 只保留在少量轻松文案或空状态中，不承担导航和状态识别。
- 图标主题来自海缸工具：滴管、比色瓶、盐度计、造浪泵、换水桶、珊瑚枝。

#### E. 字体与数字缺少层次

目前主要使用系统中文字体，很多标题依靠 `font-weight: 800`，数值也没有稳定的数字样式。

改进方向：

- 正文优先：MiSans / HarmonyOS Sans SC / PingFang SC / Microsoft YaHei。
- 数据优先：DIN Alternate / Bahnschrift / tabular numbers。
- 数字统一开启等宽数字特性，趋势列表和卡片读数更整齐。
- 正文不宜过细；深色模式正文至少保持中等对比。
- 后续若上架 App，再把选定字体作为本地资源打包，避免依赖在线字体。

#### F. 桌面端只是“放宽的手机页面”

当前主容器最大宽度约 780px，桌面端仍是单列长页面。这在早期开发很安全，但会强化网页表单感。

改进方向：

- 手机继续单列。
- 平板与桌面采用 12 列网格：左侧 4 列放鱼缸状态与快捷操作，右侧 8 列放趋势和当前任务。
- 历史记录和专业分析可以形成桌面侧栏，不再全部纵向堆叠。
- 不为了填满屏幕而增加内容；宽屏的价值是并列对照，而不是把卡片拉得更宽。

#### G. 深色主题不能直接靠“反转颜色”完成

当前文件中仍有较多 `#fff`、浅色渐变、内联背景和组件专属颜色。直接添加 `prefers-color-scheme` 会出现白块、图表刺眼和状态色失真。

改进方向：

- 先把颜色改为语义 Token，再做深色主题。
- 图表、遮罩、弹窗、输入框、Toast、阴影都必须使用 Token。
- 深色模式减少阴影，主要靠表面亮度差和边框建立层级。

#### H. PWA 外观还有一个技术细节

页面中的 `<meta name="theme-color" content="var(--ocean)">` 不能依赖 CSS 变量解析，应在主题切换时写入真实色值。否则系统状态栏颜色可能与页面主题不一致。

---

## 三、共用设计骨架

两套主题只更换皮肤，不更换信息结构。建议先确定以下语义 Token：

```text
--bg-canvas            页面背景
--bg-elevated          导航、弹窗等浮层
--surface-primary      主要卡片
--surface-secondary    次级分组
--surface-muted        说明、空状态、只读区域
--text-primary         主要文字
--text-secondary       次要文字
--text-tertiary        辅助文字
--brand-primary        主品牌色
--brand-secondary      辅助品牌色
--signal-ok            正常
--signal-watch         留意
--signal-risk          风险
--signal-info          中性信息
--border-subtle        普通边框
--border-strong        选中与强调边框
--focus-ring           键盘焦点
--chart-grid           图表网格
--chart-reference      参考区间
```

### 3.1 组件层级

| 层级 | 用途 | 视觉规则 |
|---|---|---|
| L0 页面背景 | 营造水下环境 | 不运动或只保留极慢变化；不能影响阅读 |
| L1 页面分区 | 水质、补充、滴定等主任务 | 无大阴影，靠间距与标题分组 |
| L2 焦点卡 | 今日结论、当前计算结果 | 每屏最多一个；允许品牌色和签名组件 |
| L3 数据卡 | KH、钙、镁、NO₃、PO₄ | 统一规格，强调数字与趋势，不堆装饰 |
| L4 折叠层 | 历史、证据、设置、专业说明 | 默认安静，需要时展开 |
| L5 浮层 | 编辑、估算、确认 | 背景明确、返回路径清楚、支持系统返回键 |

### 3.2 手机信息结构

```text
┌──────────────────────────┐
│ ReefPal      鱼缸切换/主题 │  56px 紧凑应用栏
├──────────────────────────┤
│ 我的鱼缸 · LPS · 46L       │  44px 鱼缸上下文条
├──────────────────────────┤
│ 礁况线 Reef Pulse          │  首页展开，其他页压缩
│ 今日结论 / 下一件事         │
├──────────────────────────┤
│ 当前模块标题     主要动作    │
│ 当前模块内容                │
│                            │
├──────────────────────────┤
│ 水质   换水   补充   滴定    │  固定底部导航
└──────────────────────────┘
```

### 3.3 桌面信息结构

```text
┌──────────────┬─────────────────────────────────┐
│ 品牌/鱼缸信息  │ 礁况线 + 今日结论 + 快捷动作      │
│ 四个主导航     ├─────────────────┬───────────────┤
│ 最近维护       │ 当前模块主工作区   │ 趋势/历史/依据  │
│ 数据备份       │                 │               │
└──────────────┴─────────────────┴───────────────┘
```

### 3.4 签名组件：礁况线 Reef Pulse

礁况线不是装饰进度条，而是 ReefPal 的“今日海况仪表”：

- 中央稳定带：当前记录处于本缸参考范围且趋势平稳。
- 左侧资料不足带：记录缺失、过期或无法判断。
- 右侧留意带：偏离参考、异常波动、到期维护或待复测。
- 标记点：当前整体状态。
- 小节点：待办、复测、换水、滴定调整等时间事件。
- 点击后展开判断依据，默认只露出一句结论和下一步。

这条线可以在亮色模式中像水面折光，在深色模式中像生物荧光。它应成为 ReefPal 截图里一眼能认出的元素。

---

## 四、视觉方案 A：阳光潮池 · Reef Field Notes

### 4.1 气质

清亮、可信、亲近，但不是儿童科普风。像白天在自然光下整理水质记录：有海水的通透感，也有水族观察手册的严谨。

适合作为默认主题，尤其适合新用户、数据录入和长时间阅读。

### 4.2 色彩

| 名称 | 色值 | 用途 |
|---|---:|---|
| Pearl Canvas | `#F4F8F7` | 页面背景 |
| Foam Surface | `#FCFEFD` | 主要卡片与输入区 |
| Lagoon | `#0B7180` | 主按钮、选中态、品牌 |
| Deep Current | `#123B47` | 主文字、深色标题 |
| Sea Glass | `#D9ECE7` | 选中背景、参考区间 |
| Coral Signal | `#E36F5C` | 风险与关键提醒，不作大面积装饰 |
| Sunlit Sand | `#F2C66D` | 到期、留意、时间节点 |
| Kelp Green | `#2B8A68` | 稳定、完成 |

### 4.3 背景

- 基底使用偏青的珍珠白，不再使用上下均匀灰蓝渐变。
- 左上和右下放两组非常淡的水下焦散光斑，透明度控制在 4%–7%。
- 局部加入盐度计刻度般的细短线，不能形成明显网格。
- 删除漂浮大水滴和持续波浪动画；只在首页顶部保留一次缓慢的光线扫过礁况线。
- 减少大面积玻璃模糊，避免页面看起来像通用 SaaS 模板。

### 4.4 卡片与控件

- 主要卡片圆角 18px；数据小卡 14px；按钮 12px；不要所有元素都做胶囊。
- 普通卡片主要靠 1px 淡边框区分，只有今日焦点卡使用轻阴影。
- 数字比单位至少大两个字号层级，单位和参考范围降低对比。
- 输入框使用微暖白底，聚焦时出现 Lagoon 色双层焦点环。
- 主要按钮使用纯 Lagoon，不再叠加过强渐变。
- 次要按钮使用透明底和边框，危险操作使用文字色与确认层，不直接满屏红色。

### 4.5 图标与插图

- 使用圆角线性 SVG 图标，线宽保持 1.75–2px。
- 主导航：水质试剂瓶、换水循环箭头、滴管、滴定泵。
- 空状态可以使用极简线稿：空比色瓶、未打点的趋势线、等待复测的小珊瑚枝。
- 不使用写实鱼类照片作为日常背景，以免和用户自己的缸形成审美冲突。

### 4.6 礁况线表现

- 背景像浅水折光：资料不足灰蓝、稳定海玻璃绿、留意沙金、风险珊瑚橙。
- 标记点像盐度计浮标，保持实心、清晰，不做强发光。
- 展开后依据卡类似观察笔记，而不是系统日志。

### 4.7 亮色版应避免

- 过多薄荷绿导致“医疗 App”感。
- 米白、衬线大标题和陶土色组合，容易变成生活方式模板。
- 到处出现珊瑚剪影，反而削弱数据工具的可信度。
- 用浅灰文字换取“高级感”，海边强光下会难读。

---

## 五、视觉方案 B：午夜礁盘 · Night Reef Console

### 5.1 气质

像夜间查看海缸、校准设备和观察珊瑚伸展时的控制台。沉静、专业、有少量生物荧光，但不能做成电竞霓虹界面。

适合夜间维护、重度玩家、OLED 设备，也能成为 ReefPal 更有传播力的展示主题。

### 5.2 色彩

| 名称 | 色值 | 用途 |
|---|---:|---|
| Abyss Canvas | `#061820` | 页面背景 |
| Reef Navy | `#0B2731` | 次级表面 |
| Deep Panel | `#103642` | 主要卡片 |
| Moon Foam | `#E7F2F2` | 主文字 |
| Muted Current | `#91AEB3` | 次要文字 |
| Biolume | `#4FD2C5` | 主品牌色、稳定、焦点 |
| Signal Blue | `#63AFFF` | 图表、链接、中性信息 |
| Coral Alert | `#FF7B6B` | 风险与异常 |
| Amber Tide | `#F1B75B` | 到期与留意 |

### 5.3 背景

- 使用深海蓝黑渐变，而不是纯黑，顶部略亮、底部略深。
- 只保留少量极小浮游光点，静止或极慢漂移；数量要少于 12 个。
- 用一条很淡的水下焦散弧线贯穿顶部，与礁况线呼应。
- 不使用大面积蓝紫渐变和强光晕，防止变成加密货币或电竞产品。
- OLED 下大面积背景可接近黑色，但卡片仍需与背景拉开亮度差。

### 5.4 卡片与控件

- 主要靠表面亮度与 1px 半透明边框分层，阴影接近零。
- 输入框略比卡片深，聚焦时使用 Biolume 焦点环。
- 稳定状态不整卡发绿，只点亮数字旁的短线或状态点。
- 风险状态使用 Coral Alert，但禁止长段正文全红。
- 图表网格降低到 8%–12% 不透明度，参考区间使用低亮度色带。
- 弹窗背景应实，不使用过度透明玻璃，以保证数值可读。

### 5.5 礁况线表现

- 稳定区像一段柔和生物荧光，只有当前标记附近有轻微光晕。
- 风险节点显示珊瑚红短脉冲；遵守 `prefers-reduced-motion` 时完全静止。
- 复测、换水、维护节点像潜水仪表刻度，精确但不拥挤。

### 5.6 深色版应避免

- 满屏霓虹边框和高饱和渐变。
- 所有元素都发光，导致真正风险状态没有区别。
- 纯黑卡片配纯白正文，长时间阅读容易疲劳。
- 直接把亮色阴影、半透明白卡翻成黑色，层级会失控。

---

## 六、两版如何共存

### 6.1 推荐主题策略

1. 首次进入默认跟随系统主题。
2. 设置中提供“跟随系统 / 阳光潮池 / 午夜礁盘”。
3. 用户手动选择后保存选择，不再被时间或系统自动覆盖。
4. 不建议默认按日落时间自动切换；夜间开蓝灯并不代表用户一定想看深色界面。
5. 图表实例在主题切换后统一刷新配色，但不重新请求或丢失当前筛选。

### 6.2 实现原则

- 使用 `data-theme="light|dark"` 和语义 Token，不复制两份 CSS 组件。
- 所有硬编码 `#fff`、局部浅色渐变、内联背景逐步迁移到 Token。
- ECharts 使用 `getChartTheme()` 统一生成坐标轴、图例、网格、色带和 Tooltip 颜色。
- `theme-color` Meta、Manifest 背景、状态栏和启动画面分别配置真实色值。
- 先完成亮色组件收口，再补深色 Token；不要同时边改组件边猜两套颜色。

### 6.3 推荐选择

产品默认使用“阳光潮池”，同时把“午夜礁盘”作为正式可选主题一起发布。原因：

- 亮色更适合第一次接触复杂海缸参数的新用户。
- 深色为夜间维护提供真实价值，不只是炫技。
- 两套主题共用“礁况线”，能形成一个品牌、两种环境，而不是两款不同软件。

---

# Plan

接下来三个阶段都围绕同一个目标：先让现有核心功能形成可靠闭环，再验证它是否真的适合手机长期使用，最后补齐安装、离线、备份和升级能力。图鉴、开缸教程、设备百科和社区内容暂时不进入这三个阶段，以免产品主线再次扩散。

## Scope

- In：一次性元素补充记录与复测闭环、今日状态联动、手机真实场景验收、UI 主题系统、PWA 基础、数据备份与安全升级。
- Out：多缸切换、账户系统、云同步、社区、AI 问答、完整图鉴、原生商店封装、治疗用药自动剂量。

## Action items

- [ ] 建立一次性补充记录模型，完成“计算—实加—待复测—复测结果”闭环。
- [ ] 将一次性补充任务接入今日海况、礁况线和维护节奏，不与长期滴定记录混用。
- [ ] 把当前页面整理为语义化 Token 和统一组件，实施阳光潮池主题并接入午夜礁盘主题。
- [ ] 在真实 Android、鸿蒙和 iPhone 浏览器上完成核心任务验收，修复首屏效率、键盘、弹窗、图表和安全区问题。
- [ ] 拆分过大的前端文件，建立可维护的样式、组件和业务脚本边界，但不在此阶段重写框架。
- [ ] 增加 Service Worker、离线壳、更新提示和真实安装能力，明确局域网 HTTP 与 HTTPS 的限制。
- [ ] 为备份导入增加预检、自动快照、结构迁移、恢复摘要和失败回滚。
- [ ] 建立应用版本、前端构建版本、数据库结构版本和缓存版本的统一升级协议。
- [ ] 为三阶段补齐 API、数据迁移、离线、移动端和文案回归测试，并建立发布前检查表。

## Open questions

- 视觉实施时，是先只发布阳光潮池，还是亮暗两套在同一稳定版本一起发布？本文推荐一起发布，但先完成亮色组件收口。
- 一次性补充记录是否需要支持“市售液体产品”的瓶身浓度与品牌字段？本文推荐首版支持可选产品名和实际单位，但不建立公共产品库。
- 第三阶段测试期是否能提供一个 HTTPS 地址？如果继续只用局域网 HTTP，真实 PWA、Service Worker 和跨设备体验会受限制。

---

## 七、阶段一：一次性补充闭环

### 7.1 阶段目标

把当前的：

```text
测到偏低 → 计算补多少
```

补成：

```text
测到偏低
  → 选择原料/产品并计算
  → 确认实际添加量
  → 自动进入待复测
  → 今日海况提醒复测
  → 新水质记录关联本次补充
  → 判断回到参考范围、变化不足或变化超出预期
  → 结束，或进入下一轮小幅调整
```

闭环的意义不是多保存一条记录，而是让 ReefPal 从计算器变成“记得上一件事、知道下一步是什么”的养缸助手。

### 7.2 低负担原则

- 计算页面已有的信息自动带入，用户不重复填写水量、元素、实测值和计算量。
- 用户只需要确认“实际加了多少”；时间默认现在，复测时间自动推荐。
- 产品名和备注可选，不强迫建立库存或品牌档案。
- 同一元素有未完成补充时，新的计算要提醒，但不禁止继续记录。
- 首页只展示最接近到期、风险最高的一项，不堆任务清单。
- 复测完成后自动收口，不要求用户额外点击“任务完成”。

### 7.3 建议的数据状态

```text
draft          只完成计算，尚未记录实际添加
completed      已确认实际添加，等待生成复测节点
awaiting_test  等待复测
retested       已关联复测记录，等待结果判断
resolved       已回到参考范围或用户决定结束
cancelled      未执行或手动取消
```

状态不用全部暴露给用户。界面只需要说：

- 还没添加
- 等待复测
- 已复测
- 这一轮结束

### 7.4 推荐数据模型

首版增加单表 `supplement_events`，不复用 `dosing_log`：长期滴定表达“每天维持”，一次性补充表达“单次纠偏”，两者的结果判断和复测节奏不同。

建议字段：

| 字段 | 用途 |
|---|---|
| `id` | 主键 |
| `tank_id` | 归属鱼缸 |
| `element` | KH / 钙 / 镁 / NO3 / PO4 或后续可扩展元素 |
| `additive_name` | 计算时选择的原料 |
| `product_name` | 可选，用户自己的商品名 |
| `dose_form` | powder / solution / product |
| `calculated_amount` | 计算建议量 |
| `actual_amount` | 实际添加量 |
| `amount_unit` | g / ml |
| `pre_value` | 补充前实测值快照 |
| `target_value` | 本轮计算目标快照 |
| `reference_low/high` | 当时本缸参考范围快照 |
| `recorded_at` | 实际添加时间 |
| `recommended_retest_at` | 推荐复测时间 |
| `status` | 当前状态 |
| `retest_record_id` | 关联的水质复测记录，可空 |
| `note` | 用户备注 |
| `created_at/updated_at` | 审计与升级 |

为什么保存“快照”：以后用户修改缸型或自定义参考范围，历史补充判断仍应保留当时依据，不能被新设置悄悄改写。

### 7.5 API 框架

建议新增：

```text
POST   /api/supplement/events
GET    /api/supplement/events
GET    /api/supplement/events/pending
PUT    /api/supplement/events/{id}
POST   /api/supplement/events/{id}/complete
POST   /api/supplement/events/{id}/cancel
POST   /api/supplement/events/{id}/link-retest
```

也可以把 `complete/cancel/link-retest` 合并为一次 `PUT`，但显式动作接口更容易校验状态转换，避免“已取消记录又被误关联复测”。

所有写入接口应继续：

- 校验有限数字和正数。
- 校验元素与单位是否匹配。
- 校验日期不能荒谬地晚于当前时间。
- 限制备注和产品名长度。
- 返回当前事件状态及下一步，不只返回 `ok: true`。

### 7.6 复测时间方案

首版不要把复测时间包装成医学式精确结论。建议只提供保守起始值，并允许用户修改：

| 元素 | 起始复测建议 | 说明 |
|---|---|---|
| KH | 12–24 小时 | 取决于补充方式、流量和测试稳定性 |
| 钙 | 24–48 小时 | 避免刚加完就在局部高浓度状态下测试 |
| 镁 | 24–48 小时 | 大幅调整尤其需要分次 |
| NO3 / PO4 | 24–72 小时 | 结合系统反应、投喂和检测下限 |

系统使用一个默认时间点，例如区间上限；界面说明这是“复测提醒”，不是保证反应完成的绝对时间。

### 7.7 复测关联方案

#### 方案 A：用户从补充卡点“去复测”

- 自动带入元素。
- 保存水质后直接关联事件。
- 最明确，误关联概率最低。

#### 方案 B：系统自动匹配下一条同元素记录

- 操作最少。
- 如果用户只是校验试剂或记录了更早日期，可能误关联。

#### 推荐：A 为主，B 为辅助提示

用户从任务进入时自动关联；如果用户直接在水质页新增同元素记录，系统提示“这组数据要作为 8 月 19 日补充后的复测吗？”，用户一键确认。

### 7.8 结果判断

结果不只看“是否落入范围”，还要看变化方向和实际添加量：

- `in_range`：回到当时参考范围，本轮结束。
- `improved`：方向正确但仍未进入范围，建议先观察或下一轮小幅调整。
- `flat`：变化小于测试误差或无明显变化，先核对原料、配液、实际水量和测试方法。
- `overshoot`：越过目标或参考上限，暂停继续补充并复测。
- `opposite`：变化方向与预期相反，优先排查测试差异、换水和其他补充来源。
- `unknown`：缺少前值、单位不一致或复测间隔不合适，不强行下结论。

首版不尝试根据一次补充反推出“真实水量”或“产品实际浓度”。一次数据受测量误差和系统消耗影响太大，容易制造虚假精度。

### 7.9 页面联动

#### 元素补充页

- 计算结果下方增加“记下这次补充”。
- 弹出轻量确认层，只确认实际量、时间和可选产品名。
- 记录成功后原位置变为“等待复测 · 明天”。

#### 今日海况

- 到期前：显示“钙补充后，明天复测”。
- 到期当天：进入今日优先项。
- 逾期：显示“已经到复测时间”，不使用责备语气。
- 多项同时到期：按风险、逾期时长、核心元素优先级排序，首页最多展示三项。

#### 水质页

- 同元素录入时识别待复测事件。
- 保存后显示简短结果卡。
- 趋势图添加一次性补充标记，与换水和长期滴定使用不同图形。

#### 历史记录

- 独立显示一次性补充，不混在滴定启用/停用记录里。
- 可以编辑实际量、时间和备注。
- 已关联复测的记录删除前需说明会解除关联，但不自动删除水质数据。

### 7.10 实现方案比较

| 方案 | 做法 | 优点 | 风险 | 结论 |
|---|---|---|---|---|
| A 复用 dosing_log | 单次补充也写成滴定记录 | 改动小 | 语义混乱，后续统计错误 | 不推荐 |
| B 新增 supplement_events 单表 | 单次闭环独立建模 | 清晰、足够简单、易备份 | 增加一次数据库迁移 | 推荐 |
| C 建立通用 action ledger | 所有换水、补充、维护统一事件表 | 长期扩展强 | 首版抽象过度、迁移复杂 | 后续考虑 |

### 7.11 风险与边界

- 不根据通用公式自动给市售液体产品剂量，除非用户填写可靠瓶身浓度。
- 不提供铜药、福马林、碳源等高风险方案的自动剂量闭环。
- 同日存在大换水或滴定调整时，复测结果标记“可能受其他操作影响”。
- 用户修改实际水量后，历史事件不重算；新计算使用新水量。
- 导入旧备份时 `supplement_events` 缺失应视为空表，不能报错。
- 导出新备份时结构版本需要升级，并保留旧版兼容测试。

### 7.12 测试清单

- 数据表初始化、旧数据库迁移、重复初始化幂等。
- 新增、编辑、取消、删除、待复测查询。
- 无效元素、负数、NaN、无限值、过长文本。
- 复测关联和解除关联。
- 同元素多个待复测事件的选择。
- 备份导出、导入、重复导入不重复。
- 今日状态排序和最多三项限制。
- 趋势图标记与手机显示。
- 高于目标、方向相反、缺少前值等判断边界。
- 前端刷新、中断重试和重复点击不生成重复记录。

### 7.13 完成标准

- 用户从一次补充计算到记录实际添加不超过两次点击和一次确认。
- 已记录补充会自动进入待复测，不需要手工新建维护任务。
- 从今日海况进入复测后，水质记录自动带入元素并完成关联。
- 趋势图能区分换水、长期滴定和一次性补充。
- 新旧备份均能导入。
- 所有现有测试和新增测试通过。
- 界面不出现“系统保证”“绝对安全”等超出证据的表述。

### 7.14 推荐开发批次

1. 数据表、存储函数、迁移和备份。
2. API 与状态转换测试。
3. 计算页“记下这次补充”。
4. 今日海况待复测任务。
5. 水质记录关联与结果判断。
6. 趋势图和历史记录标记。
7. 手机回归与完整测试。

---

## 八、阶段二：手机真实使用验收与 UI 收口

### 8.1 阶段目标

这一阶段不是“看起来像手机”，而是验证用户在鱼缸旁边单手、湿手、夜间、网络不稳、需要快速录数时，是否仍能顺畅完成任务。

同时实施前面确定的视觉系统，让 UI 改造建立在真实任务上，而不是只换背景和颜色。

### 8.2 不建议此时重写前端框架

当前纯前端已经有完整功能和 142 项测试。为了视觉升级直接迁移 React/Vue，会同时改动 DOM、状态管理、接口调用、测试和 PWA，风险大于收益。

推荐渐进拆分：

```text
static/index.html             页面骨架与挂载点
static/css/tokens.css         两套主题 Token
static/css/components.css     按钮、卡片、表单、弹窗、导航
static/css/pages.css          今日、水质、换水、补充、滴定布局
static/js/api.js              统一请求与网络状态
static/js/state.js            当前鱼缸、主题、页面状态
static/js/today.js            今日海况
static/js/water.js            水质与图表
static/js/supplement.js       一次性补充
static/js/dosing.js           滴定
static/js/water-change.js     换水
static/js/app.js              启动、导航、事件协调
```

拆分不要求一次完成。先移 Token 和图标，再按模块拆 JS；每次拆分都保持现有行为和测试。

### 8.3 UI 改造顺序

#### 第一步：建立 Token

- 把背景、文字、表面、边框、状态、焦点、阴影和图表颜色语义化。
- 移除组件中的硬编码白色和浅色渐变。
- 确保阳光潮池主题完整后，再填写午夜礁盘 Token。

#### 第二步：压缩公共顶部

- 品牌区改为 56px 应用栏。
- 鱼缸铭牌改为 44px 上下文条。
- 水质首页显示完整礁况线；其他模块只显示压缩摘要。
- 数据备份和入门指南移入右上角菜单或“我的鱼缸”设置，不长期占据首页黄金位置。

#### 第三步：统一组件

- 统一主按钮、次按钮、文字按钮、危险按钮。
- 统一输入框、分段选择、日期、单位和只读结果。
- 统一焦点态、禁用态、加载态和错误态。
- 统一空状态、折叠区、Toast 和确认弹窗。
- 将 Emoji 导航替换为本地 SVG 图标。

#### 第四步：重做页面层级

- 水质：首页结论 → 快速记录 → 五项数据 → 深度趋势。
- 换水：配盐计算 → 记录本次换水 → 最近记录 → 海盐参考。
- 补充：待复测任务 → 核心元素 → 进阶元素 → 微量元素。
- 滴定：当前启用方案 → 每日量 → 调整目标 → 配液设置 → 历史。

#### 第五步：双主题和图表

- 主题切换即时生效。
- 图表 Tooltip、坐标轴、标记线、参考色带和数据系列同步主题。
- 深色模式检查状态色对比，不用荧光替代信息层级。

### 8.4 真实设备矩阵

至少覆盖：

| 平台 | 建议设备/浏览器 | 重点 |
|---|---|---|
| Android | Chrome，360–432px 宽 | 数字键盘、返回键、安装提示、低端机性能 |
| 鸿蒙 | 系统浏览器或 Chrome 兼容浏览器 | 安全区、字体、PWA 能力差异 |
| iOS | Safari，至少一台刘海屏 | 输入缩放、日期控件、状态栏、添加到主屏幕 |
| Windows | Chrome/Edge | 桌面双栏、键盘操作、导入导出 |
| 平板 | iPad/Android 横竖屏 | 双栏切换、图表宽度、弹窗尺寸 |

如果暂时没有全套设备，可以先使用浏览器视口模拟，再找 3–5 位真实用户覆盖缺少的平台。模拟不能替代 Safari 键盘、安全区和安装流程。

### 8.5 核心验收任务

每台设备都完整走一遍：

1. 第一次设置鱼缸，并用尺寸估算水量。
2. 记录五项水质，切换简洁/深度。
3. 查看趋势图，切元素、时间范围和参考区间。
4. 从偏低建议进入元素补充，计算并记录实际添加。
5. 从今日海况进入复测，完成闭环。
6. 配盐并记录“已换水”。
7. 设置滴定配液、计算每日消耗、启用和停用方案。
8. 修改历史记录并确认图表同步。
9. 导出完整备份，再导入一份有效测试备份。
10. 断开网络、切换 Wi-Fi、后台恢复，观察错误和数据状态。

### 8.6 重点交互检查

#### 单手与湿手场景

- 主要点击目标不小于 44 × 44px。
- 保存按钮靠近当前输入区，不要求回到页面顶部。
- 不把相邻的删除与保存放得太近。
- 关键数值支持整行点击聚焦。

#### 手机键盘

- 数值输入打开数字/小数键盘。
- 键盘弹出时当前输入和保存按钮仍可见。
- iOS 输入框字号不低于 16px，避免 Safari 自动放大。
- 关闭弹窗后焦点回到原操作位置。

#### 弹窗与返回

- 尺寸估算、鱼缸设置和维护周期使用统一底部抽屉或全屏弹层。
- Android 系统返回键优先关闭弹层，再返回上一页。
- 弹层滚动不带动背景。
- 关闭和保存都有明确结果，不依赖点击遮罩。

#### 图表

- 360px 宽仍能看清单位和日期。
- Tooltip 不被底部导航遮挡。
- 双指缩放或横向拖动不与页面左右滑动冲突。
- 数据很多时保持流畅，避免每次切 Tab 重建全部图表。

#### 弱网与断网

- GET 请求可自动短重试，写操作不自动重复提交。
- 保存中断时告诉用户这条记录有没有成功落库。
- 恢复连接后允许手动重试，不能静默生成重复记录。
- 页面恢复前台时刷新今日状态，但保留未提交的输入。

### 8.7 可用性指标

首轮不需要复杂埋点，可以人工记录：

- 新用户完成首次设置所需步骤。
- 记录一项水质所需时间。
- 从建议到完成一次补充记录的点击数。
- 用户第一次能否分清“一次纠偏”和“长期滴定”。
- 错误后是否知道如何恢复。
- 24 小时后能否找到待复测任务。
- 用户是否会主动切换简洁/深度模式。
- 用户对亮色/深色主题的选择和原因。

### 8.8 反馈收集方案

#### 方案 A：人工访谈 + 截图

最适合早期 3–5 人测试，信息密度高，不需要账号和服务端。

#### 方案 B：应用内“反馈包”

生成不含水质具体数据的诊断文本：版本、系统、屏幕、当前模块、错误摘要。用户自行复制发送。不要默认上传。

#### 方案 C：接入分析平台

现在不推荐。会引入隐私、同意、网络和应用商店披露工作，样本量也不足以支撑统计结论。

推荐先使用 A，必要时加 B。

### 8.9 视觉与无障碍验收

- 正文、按钮、状态文字满足基本对比度。
- 不能只靠红/绿区分状态，同时使用文字和图标。
- 键盘焦点在亮暗主题都清晰。
- 系统“减少动态效果”开启后，波浪、光晕、卡片进入动画停止。
- 200% 字体缩放时主要任务仍可完成。
- 深色模式下图表网格、Tooltip 和参考色带不刺眼。
- 阳光下亮色主题的辅助文字仍然可读。

### 8.10 完成标准

- 四个主模块切换后，当前模块的主要动作在手机首屏内可见。
- 360px 宽无页面横向滚动。
- 核心流程在目标 Android、鸿蒙、iOS 至少各一台真实设备通过。
- 亮暗主题覆盖页面、弹窗、图表、Toast、输入和系统状态栏。
- 用户能清楚区分水质记录、一次性补充和长期滴定。
- 弱网不会因为自动重试产生重复写入。
- UI 拆分后现有测试和新增视觉结构测试全部通过。

### 8.11 推荐开发批次

1. Token 与背景方案。
2. 顶部压缩、礁况线和底部导航。
3. 通用按钮、表单、卡片、弹窗、图标。
4. 水质和今日首页重排。
5. 换水、补充、滴定页面重排。
6. 深色主题和图表主题。
7. 真实设备测试与问题清单。
8. 按优先级修复并冻结 V0.1 UI。

---

## 九、阶段三：PWA、离线、备份与版本升级

### 9.1 先澄清当前状态

当前项目已有：

- `manifest.webmanifest`
- 192px、512px 和 Apple Touch 图标
- `display: standalone`
- FastAPI 健康检查和应用版本 0.5.0
- JSON 全量备份、CSV 导出、结构版本 v5

当前项目还没有：

- Service Worker
- 应用壳缓存
- 离线页面或只读快照
- 前端缓存版本和更新协议
- HTTPS 部署方案
- 写操作离线队列与幂等键
- 导入前自动快照和失败回滚界面

因此，“添加到主屏幕”目前主要改变启动方式和外观，并不代表真正离线。手机通过 `http://192.168.x.x` 访问时，Service Worker 的安全上下文也会受浏览器限制；真实 PWA 测试最好使用 HTTPS。

### 9.2 阶段目标

- 可安装：浏览器能稳定识别应用并以独立窗口运行。
- 可解释：离线、服务不可达、版本更新时，用户知道当前状态。
- 不丢数据：升级、导入和断网不能悄悄破坏记录。
- 可恢复：每次高风险数据操作前都有恢复点。
- 可演进：前端、后端、数据库和缓存版本不会各自漂移。

### 9.3 离线方案比较

| 方案 | 能力 | 优点 | 风险 | 适用阶段 |
|---|---|---|---|---|
| A 应用壳 + 最近数据只读 | 离线能打开页面，看上次同步快照，不能新增 | 简单、安全、不产生冲突 | 离线不能记录 | V0.1 推荐 |
| B 应用壳 + 写入队列 | 离线可新增，联网后同步 | 使用体验好 | 重复写入、日期顺序、冲突处理复杂 | 小范围测试后 |
| C 完整 Local-first | 手机本地数据库为主，再与云/主机同步 | 最接近原生 App | 架构重做，需要账号、同步协议和冲突模型 | 正式多端阶段 |

推荐阶段三先做 A。海缸数据写错、重复或错序比“离线不能记”更危险。等 API 加入幂等键、同步状态和冲突规则后，再考虑 B。

### 9.4 Service Worker 框架

#### 预缓存

- HTML 应用壳。
- 拆分后的 CSS 和 JavaScript。
- ECharts 本地文件。
- SVG 图标和应用图标。
- 离线说明页。

#### 运行时缓存

- 静态资源：Cache First，使用带版本文件名或缓存版本。
- `/api/health`：Network Only 或极短缓存。
- 水质、今日状态等 GET：Network First，失败时返回最近只读快照。
- 所有 POST/PUT/DELETE：Network Only；首版失败时保留用户输入并提示重试，不自动排队。

#### 更新流程

1. 新 Service Worker 下载完成但不立即接管正在编辑的页面。
2. 显示“新版本已经准备好，保存当前内容后刷新”。
3. 用户确认刷新后激活新缓存。
4. 如果数据库需要迁移，后端先完成迁移并通过健康检查，再让前端进入新版本。
5. 更新失败时继续使用旧缓存，不留下半套资源。

### 9.5 HTTPS 方案

#### 方案 A：继续局域网 HTTP

适合开发调试，不适合验证真实 Service Worker 和跨设备长期使用。

#### 方案 B：局域网 HTTPS 证书

可用 `mkcert` 等方式为测试设备安装本地 CA，但每台手机都要配置，适合开发者，不适合普通试用者。

#### 方案 C：HTTPS 测试域名/隧道

适合小范围测试 PWA，但必须考虑服务暴露、访问控制和数据隐私。

#### 方案 D：Capacitor 原生壳

最终上架 Android/iOS/鸿蒙时可考虑，但它解决的是封装和原生能力，不自动解决数据同步、隐私和离线数据库。

推荐：开发阶段用 B 或 C 验证 PWA；产品闭环稳定后，再单独评估 Capacitor 与鸿蒙技术路线。不要为了商店图标过早封装一个仍依赖电脑 SQLite 服务的网页。

### 9.6 备份安全升级

#### 导出

- 保留当前完整 JSON。
- 文件名包含日期、应用版本和结构版本。
- 导出页面显示包含哪些数据、记录数量和鱼缸名称。
- 可选生成校验摘要，导入时验证文件是否完整。

#### 导入预检

导入前只解析、不写入，展示：

- 备份创建时间。
- 结构版本。
- 鱼缸名称和类型。
- 水质、换水、滴定、维护、一次性补充各多少条。
- 将新增、跳过、更新多少条。
- 是否来自未来版本或结构不兼容。

#### 自动快照

正式导入前，自动把当前数据库导出为恢复快照。快照至少保留最近 3 份，并允许用户下载。

#### 事务与回滚

- 整次导入放在数据库事务中。
- 任一结构或数据校验失败则全部回滚。
- 界面明确告诉用户“没有改动现有数据”。
- 导入成功后运行基本完整性检查，再刷新页面。

#### 兼容原则

- 旧备份缺少新表时按空数据处理。
- 新版本导入旧结构时先迁移再写入。
- 旧版本不能导入未来结构时明确阻止，不能忽略未知字段后继续。
- 所有导入保持幂等，重复导入不重复生成记录。

### 9.7 统一版本协议

建议同时维护四个版本：

| 版本 | 示例 | 作用 |
|---|---|---|
| `app_version` | 0.6.0 | 对用户和发布说明 |
| `frontend_build` | 20260819.1 | 判断静态资源是否一致 |
| `schema_version` | 6 | SQLite 与备份迁移 |
| `cache_version` | reefpal-v6 | Service Worker 缓存淘汰 |

`/api/health` 建议返回：

```json
{
  "status": "ok",
  "app_version": "0.6.0",
  "schema_version": 6,
  "api_compat": 1
}
```

前端启动时比较自身构建期望与后端版本：

- 兼容：正常启动。
- 后端过旧：提示更新服务端，不继续写入。
- 前端缓存过旧：提示刷新。
- 数据库迁移失败：只读启动，保留备份出口。

### 9.8 数据库迁移原则

- 每个结构版本只做一次、可检测、可测试的迁移。
- 迁移前备份数据库文件或导出 JSON 快照。
- 迁移使用事务；失败后回滚到旧结构。
- 不在普通请求处理中偷偷执行复杂迁移。
- 启动日志记录迁移前后版本，但不记录用户具体水质数据。
- 为至少最近两个备份版本保留导入兼容测试。

### 9.9 如果以后加入离线写入

必须先补：

- 每条写操作的客户端 UUID 幂等键。
- `created_at` 与 `recorded_at` 分开。
- 同一记录在手机和服务端同时编辑时的冲突提示。
- 待同步、同步中、失败、已同步状态。
- 用户可查看并手动重试失败队列。
- 删除操作使用墓碑记录，不能只在本地直接消失。

没有这些基础，不建议把写操作缓存起来“等联网自动提交”。

### 9.10 应用商店前的关系

完成阶段三，不代表已经可以直接上架，但会得到一个更可靠的 Web/PWA 内核。之后进入商店准备时还需独立处理：

- Android、iOS、鸿蒙的封装方案。
- 应用签名、包名、图标、启动画面。
- 隐私政策、数据说明、网络访问说明。
- 权限最小化和应用商店截图。
- 崩溃日志与用户反馈渠道。
- 如果数据离开本机，还需账户、云存储、加密、删除和导出机制。

当前 SQLite 数据保存在运行服务的电脑上。即使把网页封装成手机 App，只要数据层没有迁移，手机仍然依赖这台电脑和局域网。因此商店封装必须晚于数据架构决策。

### 9.11 测试清单

- 首次安装、重复安装、从主屏幕启动。
- 亮暗主题下状态栏、启动背景和图标。
- 静态资源离线加载。
- GET 请求回退到最近只读快照。
- 写操作离线时不重复、不丢失输入。
- 新 Service Worker 等待、激活和回滚。
- 缓存旧前端连接新后端、缓存新前端连接旧后端。
- 数据库从 v5 升级到新版本。
- 旧备份导入、新备份重复导入、损坏文件、未来结构文件。
- 导入失败后的事务回滚和自动快照恢复。
- iOS 添加到主屏幕、Android 安装、鸿蒙浏览器差异。

### 9.12 完成标准

- HTTPS 环境中通过 PWA 安装检查。
- 无网络时应用能打开并清楚标记“只读的最近数据”。
- 离线写操作不会被系统假装保存成功。
- 更新不会在用户填写表单时强制刷新。
- 导入任何备份前都有预检和自动恢复点。
- 数据库迁移失败时旧数据保持可恢复。
- 前端、后端、结构和缓存版本不一致时有明确处理。
- 阶段三所有自动化和真实设备测试通过。

### 9.13 推荐开发批次

1. 版本协议与健康检查扩展。
2. 备份预检、自动快照、事务回滚。
3. 数据库迁移框架和结构版本升级。
4. 前端静态资源拆分与带版本缓存。
5. Service Worker 和只读离线壳。
6. 更新提示和安全刷新流程。
7. HTTPS 测试环境。
8. Android、iOS、鸿蒙安装与更新验收。

---

## 十、三阶段之间的依赖

```text
阶段一：一次性补充闭环
   │ 产出 supplement_events、复测任务、结果判断
   ▼
阶段二：手机验收与 UI 收口
   │ 用真实闭环验证导航、首屏、表单和双主题
   ▼
阶段三：PWA 与数据可靠性
   │ 把已经稳定的流程做成可安装、可升级、可恢复的内核
   ▼
V0.1 小范围发布
   │
   ├─ 根据真实反馈修核心链路
   └─ 再进入图鉴/教程/设备知识与应用商店封装
```

不能调换的关键点：

- 如果阶段一没闭环，阶段二只能测试一组零散计算器。
- 如果阶段二没完成，阶段三会把尚未稳定的网页包装得更牢，后续返工更贵。
- 如果阶段三的数据迁移与恢复没做好，就不适合把长期记录交给更多真实用户。

---

## 十一、建议的版本节点

版本号只是建议，具体可按现有发布习惯调整。

| 节点 | 主要内容 | 是否适合邀请用户 |
|---|---|---|
| 0.5.x | 当前功能与文案收口 | 仅自己使用 |
| 0.6.0 | 一次性补充闭环 | 1–2 位熟悉的鱼友 |
| 0.7.0 | 新 UI、亮暗主题、手机验收 | 3–5 位不同经验用户 |
| 0.8.0 | PWA、备份预检、版本迁移 | 小范围持续使用 |
| 0.9.0 | 根据真实反馈修正核心流程 | 公开测试候选 |
| 1.0.0 | 数据架构和商店封装路线确定 | 再决定正式上架节奏 |

---

## 十二、进入图鉴与知识功能前的门槛

以下条件大部分满足后，再扩展图鉴、教程和设备知识库更稳：

- 用户连续两到四周愿意回来记录，而不是只计算一次。
- 一次性补充、复测、换水和滴定能形成可解释的时间线。
- 新用户能分清四个主模块，不依赖开发者讲解。
- 手机端没有阻断核心任务的问题。
- 数据可以备份、恢复和安全升级。
- 产品知道哪些问题需要工具解决，哪些问题需要知识内容解释。

到那时，知识功能不应成为第五个巨型 Tab，而应嵌入任务：

- 记录 PO₄ 时解释检测下限。
- 选择 LPS 时展示适合该阶段的入门内容。
- 设置蛋分维护时链接设备原理和清洁方法。
- 补钙时解释钙、KH、镁之间的关系。

这样才能保持 ReefPal“一键、聪明、负担小”的方向，同时逐步长成真正深入浅出的海缸平台。
